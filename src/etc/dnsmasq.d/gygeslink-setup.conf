# GygesLink — dnsmasq setup (OBSOLÈTE — Radxa Zero)
#
# Ce fichier n'est plus utilisé.
# Le mode setup se fait désormais via le portail Flask accessible
# directement sur usb0 (192.168.100.1:443) depuis le PC branché en USB-C.
# Le DHCP pour le PC est géré par dnsmasq sur usb0 (config principale).
