# GygesLink — NetworkManager : randomisation MAC WiFi
#
# NOTE : wlan0 est géré par netplan (macaddress: shuffle dans le yaml).
# Cette config NM est conservée pour les éventuels autres périphs WiFi.
# NM ne gère pas wlan0 (unmanaged via systemd-networkd).

[device]
wifi.scan-rand-mac-address=no

[connection]
wifi.cloned-mac-address=random