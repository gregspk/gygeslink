# GygesLink — Modules kernel à précharger au boot
# Chargés par systemd-modules-load.service (avant sysctl et réseau).

# nf_conntrack : nécessaire pour les paramètres sysctl conntrack
nf_conntrack

# dwc2 : contrôleur USB OTG Allwinner H618 (mode gadget sur USB-C)
dwc2

# libcomposite : framework USB composite via configfs
libcomposite

# usb_f_ncm : fonction CDC-NCM (carte réseau USB standard)
# Remplace RNDIS (obsolète sur Windows 10/11)
usb_f_ncm